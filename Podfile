# Uncomment this line to define a global platform for your project
platform :ios, '8.0'
use_frameworks!

target 'Emeal' do
  pod 'HTMLReader'
  pod 'Alamofire', '~> 1.2'
end

target 'EmealTests' do

end
